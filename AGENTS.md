# AGENTS.md

本文件用于约束本项目中人工开发者与代码代理的实现方式。除非用户明确提出不同要求，所有新增或修改的 Python 代码均应遵循本文规范。

## 1. 项目目标

本项目是一个使用 Python 3 和 PyQt6 开发的 Windows 桌面工具，主要用于：

- 创建班级材料收集活动；
- 从 Excel 导入学生名单；
- 为学生归档一个或多个文件；
- 查看学生提交状态；
- 按学生姓名生成独立 ZIP 压缩包；
- 使用 PyInstaller 打包为可独立运行的 Windows 可执行程序。

详细产品需求见 `.tan/requirements.md`。实现与该文档冲突时，以用户最新指示为最高优先级。

## 2. 基本开发原则

- 使用 Conda 环境 `py3106`（Python 3.10.6）进行本地开发和验证，代码须兼容 Python 3.10 或更高版本。
- GUI 使用 PyQt6，不混用 PyQt5、PySide 或其他 GUI 框架。
- 优先使用 Python 标准库；仅在确有必要时引入第三方依赖。
- 代码应清晰、可维护、可测试，不为尚未提出的功能进行过度设计。
- 业务逻辑与界面逻辑分离，避免将文件处理、Excel 解析或数据库操作全部写在窗口类中。
- 不静默忽略异常；应记录异常，并向用户展示可理解的中文错误信息。
- 不修改、移动或删除用户的原始材料文件；归档时默认复制文件。
- 所有可能覆盖、替换或删除数据的操作必须经过明确确认。

## 3. 建议目录结构

```text
项目根目录/
├─ AGENTS.md
├─ main.py
├─ pyproject.toml 或 requirements.txt
├─ src/
│  └─ class_committee_tools/
│     ├─ __init__.py
│     ├─ app.py
│     ├─ models/
│     ├─ services/
│     ├─ repositories/
│     ├─ ui/
│     ├─ workers/
│     └─ utils/
├─ tests/
├─ resources/
├─ packaging/
└─ .tan/
   └─ requirements.md
```

- `models`：领域模型和数据结构。
- `services`：活动管理、名单导入、文件归档和压缩导出等业务逻辑。
- `repositories`：SQLite 等持久化访问逻辑。
- `ui`：窗口、对话框和可复用控件。
- `workers`：耗时任务及其线程封装。
- `utils`：无状态、通用的辅助函数。
- `main.py` 只负责应用程序入口和顶层异常处理，不承载业务逻辑。

若项目规模暂时较小，可以简化目录，但仍须保持业务逻辑、数据访问和 GUI 代码职责清晰。

## 4. Python 编码规范

### 4.1 风格与格式

- 遵循 PEP 8 和 PEP 257。
- 使用 4 个空格缩进，禁止使用 Tab 缩进。
- 文件统一使用 UTF-8 编码和 LF 换行符。
- 建议最大行宽为 100 个字符。
- 建议使用 Ruff 进行代码检查和格式化。
- 删除无用导入、无用变量、调试输出和被注释掉的废弃代码。
- 不使用 `from module import *`。
- 不在模块导入时执行文件写入、数据库迁移或启动 GUI 等副作用操作。

### 4.2 命名

- 模块、函数和变量使用 `snake_case`。
- 类名使用 `PascalCase`。
- 常量使用 `UPPER_SNAKE_CASE`。
- 私有实现细节使用单下划线前缀，例如 `_load_students`。
- Qt 信号名称使用清晰的过去式或事件语义，例如 `import_finished`、`export_failed`。
- 名称应表达业务含义，避免 `data1`、`temp2`、`do_stuff` 等模糊命名。

### 4.3 类型标注

- 所有公共函数、方法和服务接口都应提供参数及返回值类型标注。
- 优先使用现代类型语法，例如 `list[str]`、`Path | None`。
- 文件路径统一使用 `pathlib.Path`，不要在业务层长期传递裸字符串路径。
- 复杂数据优先使用 `dataclasses.dataclass`、枚举或明确的领域模型表达。
- 除非与无类型第三方库交互，不滥用 `Any`。

### 4.4 函数与类

- 函数应职责单一，尽量保持短小。
- 避免过深嵌套；优先使用提前返回降低复杂度。
- 不使用可变对象作为默认参数。
- 优先使用依赖注入传入仓库、服务和配置，避免难以测试的全局单例。
- 公共类和重要公共函数应有简洁 docstring，说明用途及关键约束。
- 注释用于说明原因、限制和容易误解的行为，不重复翻译代码本身。

## 5. PyQt6 GUI 规范

- 只在主线程创建或更新 QWidget。
- 文件复制、Excel 解析、数据库批量操作和 ZIP 压缩等耗时任务必须放入 `QThread`、`QThreadPool` 或等价后台任务中。
- 后台任务通过信号向主线程报告进度、结果和异常，不从工作线程直接操作控件。
- 信号与槽连接应集中、清晰，避免同一信号被意外重复连接。
- 窗口关闭前正确停止或等待后台任务，避免线程仍在运行时销毁对象。
- 禁止在 UI 线程使用长时间阻塞调用或主动轮询。
- 用户可见文本默认使用简体中文，并尽量集中管理，避免同一提示散落重复。
- 使用布局管理器完成界面排版，不依赖固定绝对坐标。
- 控件应有明确标签；仅有图标的按钮应提供工具提示。
- 错误对话框应说明发生了什么、可能原因以及用户可以采取的行动。
- 表格中的提交状态不能只依赖颜色表达，应同时显示文字或图标。

## 6. 数据与持久化规范

- 结构化数据默认使用 SQLite 保存，并通过独立 repository 层访问。
- 数据库操作必须使用参数化查询，不拼接用户输入形成 SQL。
- 涉及多条关联写入时使用事务，失败后回滚。
- 为活动和学生分配不可变的内部唯一标识，不以活动名称或学生姓名作为数据库主键。
- 学生姓名只作为显示信息；存在同名学生时使用学号或内部 ID 区分。
- 数据库 schema 发生变化时，应提供明确、可重复执行的迁移策略。
- 应用数据目录通过统一配置函数获得，禁止在多个模块中硬编码路径。
- 开发、测试和生产数据必须隔离。

## 7. Excel 导入规范

- `.xlsx` 默认使用 `openpyxl` 读取。
- 读取工作簿时应优先采用只读或适当的低内存模式。
- 姓名字段应去除首尾空白，但不得擅自改变姓名内容。
- 空姓名、重复记录、字段缺失和无效工作表必须产生明确的导入结果。
- 导入过程应先校验和预览，再执行持久化写入。
- 追加或替换现有名单必须由用户明确选择。
- Excel 单元格内容均视为不可信输入，不将其作为命令、代码或未经清理的文件路径执行。
- 若支持学号，应按文本读取和保存，防止前导零丢失。

## 8. 文件与压缩处理规范

- 所有文件操作使用 `pathlib.Path`、`shutil` 和上下文管理器。
- 归档材料时复制文件，不移动或更改源文件。
- 在复制前检查源文件是否存在、是否为普通文件以及目标目录是否可写。
- 对 Windows 禁用字符、保留名称、尾随空格和尾随句点进行文件名清理。
- 对清理后为空的名称提供安全的替代名称。
- 使用内部唯一标识组织存储目录，避免直接信任学生姓名构造内部路径。
- 对最终解析路径做边界检查，防止路径穿越和写入活动目录之外的位置。
- 同名文件或同名 ZIP 不得静默覆盖；应根据用户选择覆盖、跳过或追加序号。
- ZIP 生成使用 Python `zipfile`，确保中文文件名可正常解压。
- 生成压缩包时先写入临时文件，成功后再原子替换到目标位置，避免留下不完整成品。
- 单个学生导出失败时，记录失败原因并继续处理其他学生。
- 日志不得记录文件内容，不应无必要地暴露完整个人路径。

## 9. 异常处理与日志

- 捕获具体异常类型，避免无边界的 `except Exception`；仅允许在任务边界或应用入口进行兜底捕获。
- 禁止空的 `except` 或仅使用 `pass` 忽略错误。
- 底层异常可以使用 `raise ... from exc` 转换为清晰的领域异常，同时保留异常链。
- 用户提示使用易懂的中文；技术堆栈写入日志，不直接展示在普通弹窗中。
- 使用标准库 `logging`，不得用 `print` 作为正式日志方案。
- 日志至少区分 DEBUG、INFO、WARNING 和 ERROR 级别。
- 日志中避免记录敏感数据、文件内容或不必要的完整学生名单。

## 10. 测试规范

- 使用 `pytest` 编写自动化测试。
- 新增业务功能时同步增加或更新测试。
- 重点测试：
  - 活动名称和文件名清理；
  - Excel 姓名列识别、空值和重复值；
  - 同名学生及同名文件处理；
  - 文件复制不影响原文件；
  - ZIP 内容、命名和中文文件名；
  - 数据库事务与持久化；
  - 部分导出失败时继续处理；
  - 路径穿越防护。
- 测试应使用 `tmp_path` 等临时目录，不读写真实用户数据。
- 单元测试不得依赖网络、当前用户桌面或固定磁盘路径。
- GUI 测试应覆盖关键用户流程，但优先将业务规则放入可独立测试的非 GUI 层。
- 修复缺陷时先添加能复现问题的回归测试。

## 11. 依赖管理

- 所有直接依赖必须记录在 `pyproject.toml` 或 `requirements.txt` 中。
- 开发依赖与运行依赖应明确区分。
- 不引入功能重复或长期无人维护的库。
- 添加新依赖前，应确认其许可证、Windows 兼容性以及 PyInstaller 打包兼容性。
- 禁止提交虚拟环境、构建缓存、PyInstaller 临时目录和本机配置。

建议至少忽略：

```gitignore
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
build/
dist/
*.spec.bak
*.log
```

## 12. PyInstaller 打包规范

- 日常功能修改、缺陷修复和界面调整后不得默认执行 PyInstaller 打包。
- 只有用户明确提出“打包”“生成 EXE”“生成发布版”或等价要求时，才执行打包命令。
- 用户未明确要求打包时，只运行与变更相称的自动化测试、静态检查、编译检查或无界面 GUI 验证。
- 已有 `dist` 中的可执行文件不代表最新源码版本；未执行打包时应在交付说明中避免声称 EXE 已更新。
- 打包配置应可重复执行，复杂配置使用受版本控制的 `.spec` 文件。
- 资源路径必须兼容源码运行和 PyInstaller 打包后的运行环境。
- 不在代码中硬编码开发机器的绝对路径。
- 打包前至少执行静态检查和自动化测试。
- 打包后在未安装 Python 的 Windows 10/11 环境中进行启动和核心流程验证。
- 发布前验证 Excel 导入、数据保存、材料复制、ZIP 导出及中文路径。
- 可执行文件、版本信息和图标等发布资源应集中放在 `packaging` 或 `resources` 目录。

## 13. 变更与验证要求

每次代码修改应遵循以下流程：

1. 阅读相关需求和现有实现。
2. 保留用户已有且与当前任务无关的修改。
3. 以最小、聚焦的变更实现需求。
4. 对变更运行相关测试、静态检查或最小可行验证。
5. 检查差异中是否包含密钥、个人数据、临时文件或生成产物。
6. 向用户说明完成内容、验证结果和仍存在的限制。

未经用户明确要求，不应：

- 删除活动、学生资料或用户文件；
- 重置或覆盖用户现有修改；
- 自动上传名单或材料到网络；
- 改变需求文档中已经明确的业务含义；
- 将仅适用于开发机的绝对路径写入代码或配置。

## 14. 完成标准

一项功能只有在以下条件满足时才视为完成：

- 实现符合 `.tan/requirements.md` 和用户最新要求；
- 正常流程和主要异常流程均得到处理；
- 关键业务逻辑有自动化测试；
- GUI 不因耗时任务长时间无响应；
- 不破坏用户原始文件或已有数据；
- 相关检查和测试已通过，或已明确说明无法验证的原因；
- 新增依赖、配置和使用方式已记录。
